const grid=document.querySelector('#repo-grid'),status=document.querySelector('#github-status');const user='SteppingStoneCake';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){try{const r=await fetch(`https://api.github.com/users/${user}/repos?per_page=100&sort=updated`,{headers:{Accept:'application/vnd.github+json'}});if(!r.ok)throw 0;const x=(await r.json()).filter(v=>!v.fork);status.textContent=`${x.length} public repositories from GitHub`;grid.innerHTML=x.map(v=>`<article class="repo"><h3>${esc(v.name)}</h3><p>${esc(v.description||'A project from my development portfolio.')}</p><div class="repo-meta"><span>◉ ${esc(v.language||'Mixed')}</span><span>★ ${v.stargazers_count}</span><span>⑂ ${v.forks_count}</span></div><a href="${esc(v.html_url)}" target="_blank" rel="noopener">View on GitHub →</a></article>`).join('')}catch(e){status.textContent='GitHub projects are temporarily unavailable. Use the profile link below.'}}load();